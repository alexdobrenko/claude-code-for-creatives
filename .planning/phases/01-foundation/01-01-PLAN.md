---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - archive/v1-midnight.html
  - archive/v2-fresh.html
  - archive/v3-forest.html
  - archive/v4-editorial.html
  - archive/v5-bold.html
  - archive/v6-minimal.html
  - archive/intake.html
autonomous: true

must_haves:
  truths:
    - "Visiting localhost serves the v7-colorful template as the main page"
    - "Old HTML versions are archived, not cluttering root"
    - "Project has clean structure with single entry point"
  artifacts:
    - path: "index.html"
      provides: "Main landing page (v7-colorful content)"
      min_lines: 400
    - path: "archive/"
      provides: "Archived old versions for reference"
  key_links:
    - from: "index.html"
      to: "browser"
      via: "local server"
      pattern: "Code for Creatives"
---

<objective>
Set up clean project structure with v7-colorful.html as the main index.html and archive old versions.

Purpose: Establish a working development baseline before building out sections and content.
Output: Clean project root with single index.html entry point, old versions archived.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reorganize project structure</name>
  <files>index.html, archive/</files>
  <action>
    1. Create `archive/` directory in project root
    2. Move old HTML versions to archive:
       - v1-midnight.html
       - v2-fresh.html
       - v3-forest.html
       - v4-editorial.html
       - v5-bold.html
       - v6-minimal.html
       - intake.html (the old intake form)
    3. Copy v7-colorful.html content to index.html (replacing the redirect)
    4. Remove v7-colorful.html from root (content now in index.html)

    Result: Root has clean index.html, old versions preserved in archive/
  </action>
  <verify>
    - `ls index.html` shows file exists
    - `ls archive/` shows 7 HTML files (v1-v6 + intake)
    - `ls v*.html` returns nothing (no version files in root)
    - `head -5 index.html` shows DOCTYPE and html tag (not a redirect)
  </verify>
  <done>index.html contains v7-colorful template content, archive/ contains all old versions</done>
</task>

<task type="auto">
  <name>Task 2: Verify local development works</name>
  <files>None (verification only)</files>
  <action>
    1. Start a local server with `python3 -m http.server 8000` (or check if already running)
    2. Verify the page loads by checking the server responds
    3. Document the dev workflow in a brief note

    Local dev approach: `python3 -m http.server 8000` from project root, then visit http://localhost:8000

    Note: For this simple HTML page, opening index.html directly in browser also works.
  </action>
  <verify>
    - `curl -s http://localhost:8000 | head -20` shows the HTML content starting with DOCTYPE
    - OR `open index.html` opens the page in default browser
    - Page title is "Code for Creatives"
  </verify>
  <done>Local development server works, page renders with hero section visible</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. Project root is clean (only index.html for web content, archive/ for old versions)
2. `python3 -m http.server 8000` serves the page at localhost:8000
3. Page shows "Build your own tools with AI" hero
4. All original files preserved in archive/ for reference if needed
</verification>

<success_criteria>
- index.html exists and contains full v7-colorful template (400+ lines)
- archive/ directory contains v1-v6.html and intake.html (7 files)
- No v*.html files in project root
- Local server serves the page correctly
- Page renders with hero, what you'll build, testimonial, about, CTA, and FAQ sections
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
